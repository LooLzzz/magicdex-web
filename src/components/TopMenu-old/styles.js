const useStyles = (theme) => ({
  TopMenuComponent: {
    display: 'flex',
    alignItems: 'center',
    minHeight: '9vh',
    backgroundColor: theme.palette.primary.main,
  },
});

export default useStyles;
